# Generated Apps

AgentLab creates app repositories in this folder.

Generated app folders are ignored by the root AgentLab repository by default so each app can keep its own Git history, dependencies, runtime databases, and secrets separate from the AgentLab runtime.

To publish a generated app, initialize or use the Git repository inside that app folder and push it to its own remote.
